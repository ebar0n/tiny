begin
    int a, b, x, c;
    
    x := 1;
    for(a := 0; a < 7; a := a + 1)
        x := x * 2;
        write x;
        for(b := 0; b < 2; b := b + 1)
            write b;
            for(c := 0; c < 2; c := c + 1)
                write c;
            end;
        end;
    end; 
end;


